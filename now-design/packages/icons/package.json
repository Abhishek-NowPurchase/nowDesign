{
  "name": "now-design-icons",
  "version": "1.0.3",
  "description": "SVGR React icon library for now-design.",
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp src/react/*.js dist/ && npx babel dist --out-dir dist --presets=@babel/preset-react && node scripts/generate-barrel.js",
    "fetch-icons": "node scripts/fetch-figma-icons.js"
  },
  "keywords": [
    "icons",
    "svgr",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "svgr": "^1.10.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.28.0",
    "@babel/core": "^7.28.0",
    "@babel/preset-react": "^7.27.1"
  }
}
